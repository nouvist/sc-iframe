{
  "name": "soundcloud-embed-react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "pnpm build:clean && pnpm build:tsc && pnpm build:vite",
    "build:clean": "rimraf dist types",
    "build:tsc": "tsc",
    "build:vite": "vite build",
    "prepublish": "pnpm build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "peerDependencies": {
    "soundcloud-embed": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^18.6.3",
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.0.0",
    "rimraf": "^3.0.2",
    "soundcloud-embed": "workspace:*",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}

{
  "name": "soundcloud-embed",
  "version": "0.1.0",
  "license": "MIT",
  "homepage": "https://github.com/nouvist/soundcloud-embed/#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/nouvist/soundcloud-embed.git"
  },
  "description": "Unofficial Soundcloud Widget API written in Typescript",
  "keywords": [
    "soundcloud",
    "soundcloud-api",
    "soundcloud-widget",
    "soundcloud-embed",
    "iframe"
  ],
  "main": "dist/index.js",
  "types": "types",
  "scripts": {
    "dev": "vite",
    "build": "pnpm build:clean && pnpm build:tsc && pnpm build:vite",
    "build:clean": "rimraf dist types",
    "build:tsc": "tsc -p tsconfig.types.json",
    "build:vite": "vite build",
    "prepublish": "pnpm build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^18.6.3",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}
